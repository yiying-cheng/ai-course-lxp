# 虚拟试衣 Web 产品 — Product SPEC v0.1

> 本文档为产品 SPEC，由需求梳理会话生成。技术栈占位模板见 `virtual_fitting_room_spec.md`（请勿在该文件中填写产品决策）。

---

## 一、Problem Statement（问题陈述）

### 目标用户

海外（欧美）**普通消费者**，主要在**桌面浏览器**上使用产品。

### 核心痛点

网购衣服时，仅凭商品图难以判断「穿在自己身上是否合适、好看」，导致犹豫、买错、退货成本高。

### 现状不足

- 商品模特图与自身体型、肤色、气质不一致，想象空间有限
- 到店试穿成本高，线上又缺少可信的「看自己穿上」的方式
- 现有方案（AR 镜、纯滤镜等）要么门槛高，要么不真实

### 我们要解决的问题

让用户用**自己的一张保存好的形象照** + **从网店保存/截图的衣服图**，在浏览器里快速得到**可信的静态试穿效果图**，辅助**是否购买**的决策。

### 产品边界

这不是「精确量体」或「尺码推荐」，而是一个**低门槛、可重复使用、以购买前参考为核心价值**的虚拟试衣工具。

---

## 二、Proposed Solution（方案描述）

### 2.1 产品定位

面向海外消费者的 **Desktop-first Web 虚拟试衣工具**：上传衣服图 → 基于已保存的「我的形象」生成试穿图 → 下载 / 保存历史 / 继续试下一件。

### 2.2 核心用户旅程（MVP）

```
访问网站（英文）
    ↓
[免注册] 上传：我的形象（首次）+ 衣服图 + 选择品类（上装/下装/连衣裙）
    ↓
等待 AI 生成（第三方 API）
    ↓
查看试穿结果 → 下载图片（最优先能力）
    ↓
可选：再试一件（换衣服图，复用同一形象）
    ↓
若要保存形象 / 查看历史 → 引导注册登录
    ↓
（MVP 无支付）注册后可更高额度、保存历史
```

### 2.3 功能范围（MVP 包含）

| 模块 | 说明 |
|------|------|
| **形象管理** | 首次上传并保存「我的形象」；可更新；注册后持久化 |
| **试衣** | 手动上传衣服图；支持上装、下装、连衣裙/全身；**一次试一件** |
| **结果** | 展示试穿图；**下载**（P0）；**试穿历史**（需登录）；**快速再试一件** |
| **账号** | 免注册可试；保存形象或看历史时引导注册；Google / Apple 登录 |
| **额度** | MVP 仅做基础防滥用限额（如未登录 1 次、注册后宽松月额度）；**不做付费订阅** |
| **数据** | 形象、衣服图、试穿结果在账号内长期保存；用户可手动删除 |

### 2.4 商业模式

**MVP：验证产品，暂不接入支付。**

- 全员免费使用，用**额度**控制 API 成本与滥用（具体次数待定，注册用户明显高于游客）
- **Phase 2（后续）**：再考虑 Stripe 订阅、付费档位（原「免费 + 订阅」思路保留为方向，不阻塞 MVP 上线）

### 2.5 生成目标

生成结果应尽量满足：

- 尽量保持人物脸部与身份感不变
- 尽量保持原有身体轮廓与姿势不变
- 尽量保持原始背景不变
- 尽量还原服装的关键特征：颜色、主要版型、长度、关键图案
- 优先提供「购买前参考价值」，而不是纯娱乐效果

### 2.6 对外承诺

- 输出为 **试穿预览图**，不承诺精准尺码、精准垂感或完全真实的上身还原

---

## 三、Technical Constraints（技术约束）

### 3.1 核心框架

| 项 | 约束 |
| --- | --- |
| 框架 | Next.js 16（App Router） |
| 语言 | TypeScript 5 |
| 运行时 | Node.js 24 |

### 3.2 前端技术

| 项 | 约束 |
| --- | --- |
| UI 框架 | React 19 |
| 样式 | Tailwind CSS 4 |
| 组件库 | shadcn/ui（基于 Radix UI） |
| 图标 | Lucide React |
| 状态管理 | React Context API；全局状态由 `GameContext` 承载 |
| 平台优先级 | Desktop Web 优先；Mobile 仅做基础响应式适配 |

### 3.3 AI 集成

| 项 | 约束 |
| --- | --- |
| 试穿能力 | 调用第三方 Virtual Try-On **图片 API**（不自建、不自训模型） |
| 输入 | 人物图 + 服装图；品类：上装 / 下装 / 连衣裙（一次一件） |
| 输出 | 静态试穿预览图（单张） |
| 生成耗时 | 目标 P95 端到端 **≤ 60 秒**（需 loading、进度与超时提示） |
| 质量优先级 | **效果优先**：质量与速度冲突时，优先更高质量 API 档位/参数 |

> API 供应商（如 FASHN、Vertex AI 等）开发阶段选型；需满足品类支持与海外合规披露。

### 3.4 认证与数据

| 项 | 约束 |
| --- | --- |
| 登录 | Google、Apple（邮箱等待定） |
| 支付 | **MVP 不做**（无 Stripe、无订阅结账、无账单页） |
| 额度 | 服务端校验（游客 / 注册用户）；仅防滥用，无付费档 |
| 数据存储 | 账号内长期保存试穿结果、原始人物图、原始服装图；支持单条删除 |
| 合规 | 欧美市场；GDPR 取向（隐私政策、删除权、第三方子处理方披露） |

### 3.5 开发与工程规范

| 项 | 约束 |
| --- | --- |
| 包管理器 | **pnpm**（强制，不使用 npm / yarn） |
| 代码规范 | Airbnb Style Guide |

### 3.6 输入与存储

| 项 | 约束 |
| --- | --- |
| 服装图来源 | 仅用户手动上传；不支持商品 URL 自动抓取 |
| 图片格式 | JPG / PNG / WebP（单张上限待定，建议 ≤ 10MB） |
| 形象复用 | 保存「我的形象」后，后续试穿可复用，无需每次重传 |

---

## 四、Non-goals（明确不做的事）

MVP **不包含**：

| # | Non-goal |
|---|----------|
| 1 | 商品链接抓取（Amazon / Shein 等 URL 自动取图） |
| 2 | 社交分享（Instagram / TikTok 等一键分享） |
| 3 | 原生移动 App（仅 Web） |
| 4 | AR / 实时试穿（摄像头实时预览） |
| 5 | 尺码推荐 / AI 造型建议 |
| 6 | 视频试穿（仅静态图） |
| 7 | 一次试整套（同时上衣+裤子） |
| 8 | 鞋、帽、包等配饰品类 |
| 9 | 自部署开源 VTON 模型 |
| 10 | 中国市场专属能力（微信登录、支付宝等） |
| 11 | 尺码推荐、精确身材测量 |
| 12 | 自动穿搭推荐 |
| 13 | 高精度真实性承诺（面料垂感、尺码贴合等） |
| 14 | **支付与订阅**（Stripe、定价页、订阅管理、付费额度） |

---

## 五、Success Criteria（成功标准）

### 5.1 取向

**平衡型**（效果、速度、稳定性均需及格），但 **效果为不可妥协项**：试穿图必须可信；宁可慢、可重试，也不能明显「不像我」或穿帮。

### 5.2 可量化指标（MVP 验收建议）

| 维度 | 指标 | 目标（建议） |
|------|------|----------------|
| **效果（P0）** | 主观评分「像不像我穿上」 | ≥ **4.0 / 5.0**（内测 N≥20） |
| **效果（P0）** | 「愿意用此图辅助购买决策」比例 | ≥ **70%** |
| **速度** | P95 端到端耗时 | ≤ **60 秒** |
| **稳定性** | 试穿任务成功率（非用户输入问题） | ≥ **90%** |
| **稳定性** | 失败时有明确提示 + 可重试 | **100%** 覆盖失败路径 |
| **产品** | 单次访问内「再试一件」比例 | **待定** 基线 |
| **产品** | 7 日内回访率 | **待定** 基线 |
| **商业** | 注册转化率（MVP 不看付费） | 上线后 30 天观察 |

### 5.3 单次结果成功（四项均基本成立）

1. 用户能一眼认出是自己  
2. 衣服类别正确  
3. 结果对购买决策有参考价值  
4. 画面基本自然，不明显穿帮  

### 5.4 产品层面成功

首要指标是 **重复使用率**：

- **短期**：第一张结果后，是否继续试更多衣服  
- **长期**：数日后是否再次回访  

### 5.5 核心流程验收清单

- [ ] 未登录用户完成 **1 次**完整试穿并下载结果  
- [ ] 注册用户保存形象后，第二次试穿无需重传形象  
- [ ] 注册用户可查看试穿历史  
- [ ] 额度用尽时有清晰提示（MVP 不引导付费，可提示明日再来或注册）  
- [ ] 用户可删除自己的形象 / 历史数据  

### 5.6 不作为 V1 核心指标

- 尺码准确率、身材测量准确率  
- 高风险风控识别率  
- 动态试衣完成率  
- 全服装品类覆盖率  

---

## 附录 A：需求决策记录

| 问题 | 选择 |
|------|------|
| 目标用户 | 普通消费者 |
| 核心场景 | 网购决策 |
| 衣服来源 | 手动上传 |
| 用户照片 | 保存「我的形象」，可复用、可更新 |
| 衣服品类 | 上/下装 + 连衣裙，一次一件 |
| 账号 | 先免注册，保存/历史时再注册 |
| 结果能力 | 下载（优先）+ 历史 + 再试一件 |
| 商业模式 | MVP 全免费 + 额度防滥用；支付 Phase 2 |
| 市场 | 海外英文 |
| 平台 | Desktop 优先 |
| AI | 第三方 API |
| 数据 | 账号长期保存，可删 |
| 额度 | MVP：游客 / 注册分层（具体待定）；无付费档 |
| Non-goals | 见第四节 |
| 成功取向 | 平衡 |
| 不可妥协 | **效果** |

---

## 附录 B：待定项

1. 第三方 API 选型及单次成本上限  
2. MVP 游客 / 注册用户具体额度数字  
3. Phase 2：Stripe 订阅价格、每月次数、是否年付  
4. 是否增加邮箱密码登录  
5. 免注册试穿的防滥用策略（设备 / IP）  
6. 形象照拍摄引导是否在 MVP 做  
7. GDPR 文案与 Cookie 策略  
8. 英文产品名与品牌调性  

---

## 一句话版本

面向海外普通消费者的 Desktop Web 虚拟试衣工具：用户上传保存好的形象与衣服图，生成静态试穿预览图；以购买前参考为核心，低门槛、可重复；MVP 全免费、暂不接入支付；效果优先，不做尺码推荐、链接抓取、AR/视频试穿。
